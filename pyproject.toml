[project]
name = "factory-controller"
version = "0.1.0"
description = "Durable single-host Factory mission controller"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
factory-controller = "factory_controller.cli:main"
factory-v2 = "factory_v2.cli:main"

[tool.setuptools.packages.find]
include = ["factory_controller*", "awe_worker*", "factory_v2*"]
